// Server-only copies of the hub codes. The owner code never reaches the client
// bundle; the access code is validated here as well as in the UI.
export const ACCESS_CODE = "flamehub1243";
export const OWNER_CODE = "fh32897f";
